version: '3.8'

services:
  linode-ddns:
    build: .
    container_name: linode-ddns
    restart: unless-stopped
    environment:
      - PAT=${PAT}
      - CONFIG_DIR=/data
      - CHECK_INTERVAL=300
      - CONTINUOUS_MODE=true
    volumes:
      - ./data:/data
    # Explicitly run as non-root user (UID/GID 1000)
    user: "1000:1000"
    healthcheck:
      test: ["CMD", "pgrep", "-f", "update.sh"]
      interval: 60s
      timeout: 10s
      retries: 3
      start_period: 10s

